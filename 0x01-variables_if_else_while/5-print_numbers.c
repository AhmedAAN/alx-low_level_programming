#include <stdio.h>

/**
 * main - Entery piont
 *
 * Description: 'sdfsdfsdf'
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char ch;

	for (ch = '0'; ch <=9; ch++)
	{
		putchar(ch);
	}
	putchar('\n');

	return (0);
}
